# ==============================
# GlobeTrotter MCP Chatbot Requirements
# ==============================

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# HTTP client (calls existing GlobeTrotter APIs)
httpx>=0.27.0

# Configuration & validation
python-dotenv>=1.0.1
pydantic>=2.9.0
pydantic-settings>=2.5.0

# LLM
groq>=0.11.0

# MCP
mcp>=1.0.0
# or fastmcp if preferred later

# Logging helpers (stdlib is enough, but these help)
# (no extra packages required for rotating logs)

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx  # already listed, used for TestClient too
